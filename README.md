# IOT-Network-Simulator
Design and development of a simple IoT Network Simulator that models communication between multiple IoT sensors, a gateway, and a server. The system generates sensor data, transmit the data through the network, display the data flow, and calculate basic network parameters such as packet loss and delay.
